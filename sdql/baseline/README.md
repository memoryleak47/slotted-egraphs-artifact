# SDQLite with EQSAT

## Compilation

```
cd ..
git clone https://github.com/amirsh/egg.git
cd egg
git checkout slotted-db
cd ../sdqlite-eqsat
```

## Running

`./run.sh sexp_filename`

For example:

`./run.sh batax_v0`
